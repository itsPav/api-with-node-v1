header.circle--header(style=`background-image:url(${twitterHeader}`)
    .bounds
        .circle--fluid
            .circle--fluid--cell
                a.logo(style=`background:url(${twitterURL}`)
                    h1= `@${twitterName}`
            .circle--fluid--cell.align-right
                nav
                    ul.circle--inline
                        li
                            a Sign Out